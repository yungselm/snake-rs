# My current Zen-Coding project
## Goal
Familiarize myself with `iced` by implementing simple backend logic (here Snake) and then learn about the Elm architecture and continuously implement
more formatting features as soon as the backend logic is solid.

Additionally, deepen understanding for my current weaknesses of matching statements and iterators by implementing them whenever possible.

## Setup
On WSL2 run with:

```bash
WAYLAND_DISPLAY= cargo run
```
Move the snake with arrow keys: <kbd>↑</kbd> <kbd>↓</kbd> <kbd>←</kbd> <kbd>→</kbd>
Currently panics instead of game over.